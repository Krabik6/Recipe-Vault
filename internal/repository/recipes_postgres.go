package repository

//func AddRecipe(recipe models.Recipe, db *sqlx.DB) (int, error) {
//	result, err := db.Exec("INSERT INTO recipes (id, name, description) values (?, ?, ?)", recipe.Id, recipe.Name, recipe.Description)
//}
